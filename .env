# Committed, non-secret defaults for dev and local builds (targets the staging grow-api). Excluded from Docker.
# Secrets and personal overrides go in .env.local (see .env.example), which takes precedence.
NEXT_PUBLIC_CONTACT_EMAIL=garcia.andreas.1991@gmail.com
NEXT_PUBLIC_GTMT_API_ROOT_SEGMENT=v1
NEXT_PUBLIC_APP_VERSION=dev
NEXT_PUBLIC_SENTRY_IS_ACTIVE=false
AUTH_TRUST_HOST=true
